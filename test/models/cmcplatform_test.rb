require 'test_helper'

class CmcplatformTest < ActiveSupport::TestCase
  test "platform name and version must be unique" do
  	assert false
  end

  test "Only allow name to be letters, spaces and hyphens" do
  	assert false
  end

  test "Only allow slug to be lowercase letters and hyphens" do
  	assert false
  end
end
